<% breadcrumb :edit_space, space %>

<%= render "tab_group", space: space do %>
  <%= render "tab", label: "General", url: edit_space_path(space) %>
  <%= render "current_tab", label: "People", url: space_memberships_path(space) %>
<%- end %>

<%= render "invitations", space: space %>

<%= render "memberships", space: space %>
