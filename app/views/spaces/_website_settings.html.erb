<%= render CardComponent.new(classes: "mt-3 gap-y-3") do %>
  <header>
    <h3><%= t('website_settings.index.link_to') %></h3>
    <p class="text-sm italic"><%= t('website_settings.help_text') %></p>
  </header>

  <% # TODO: move label next to checkbox  %>
  <%= form_with model: space.location do |form| %>
    <%= form.check_box :enforce_ssl %>
    <%= form.label :enforce_ssl, "Enforce SSL" %>
  <% end %>
<% end %>
