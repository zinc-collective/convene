<section>
  <div class="flex flex-wrap">
    <%= render policy_scope(journal.entries) %>
  </div>

  <%- if policy(journal.entries.new).new? %>
    <footer class="text-center">
    <%= link_to "New Journal Entry", [:new, journal.space, journal.room, journal, :entry] %>
    </footer>
  <%- end %>
</section>
